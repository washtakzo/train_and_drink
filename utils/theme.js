import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#006d77",
      contrastText: "#edf6f9",
    },
    secondary: {
      main: "#e29578",
      contrastText: "#edf6f9",
    },
  },
});

export default theme;
